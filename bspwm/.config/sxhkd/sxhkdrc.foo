#
# Defines keymap for sxhkd
#

# Application launcher {{{

super + Return
  termite
super + shift + Return
  urxvt -e zsh

super + d
  $LOCAL_BIN/rofi_run

alt + Mode_switch
  $LOCAL_BIN/rofi_run

# # vi; launch vimperator
alt + Mode_switch + v
  $LOCAL_BIN/apps/vimperator

super + F12
  authy
super + F11
  lastpass
super + F10
  vimperator
super + F9
  pcmanfm
# }}}
